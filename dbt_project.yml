name: 'tech_ecosystem_observatory'
version: '1.0.0'
config-version: 2

profile: 'tech_ecosystem_observatory'


model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  tech_ecosystem_observatory:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: mart